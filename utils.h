#ifndef UTILS
#define UTILS

#define DEBUG false

enum TraversalAlgorithms { BFS = 1, DFS = 2 };
enum ReplacementPolicies { LRU = 1, FIFO = 2, LFU = 3, RR = 4 };
enum GraphDataFormats { LIST = 1, MAT = 2 };

#endif
